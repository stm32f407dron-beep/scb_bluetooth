<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="skb_home.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:skb_home"
    Title="skb_home">

    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />



    <!--<FlyoutItem Title="Меню">
        <ShellContent Title="Меню" ContentTemplate="{DataTemplate local:MainPage}" Route="MainPage"/>
    </FlyoutItem>
    <FlyoutItem Title="Настройки">
        <ShellContent Title="Настройки" ContentTemplate="{DataTemplate local:Settingpage}" Route="Settingpage"/>
    </FlyoutItem>-->





</Shell>
